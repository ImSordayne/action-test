name: test
on: [push, workflow_dispatch]
jobs:
  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: TheMrMilchmann/setup-msvc-dev@v4.0.0
        with:
          arch: x64
      - uses: lukka/run-vcpkg@v11.5
        with:
          vcpkgCommitId: '544a4c5c297e60e4ac4a5a1810df66748d908869'
      - name: 测试构建环境
        shell: cmd
        run: |
          cmake --version
          cl
          ninja --version
          vcpkg --version
          echo %vcpkg_root%

  test-osx:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: lukka/run-vcpkg@v11.5
        with:
          vcpkgCommitId: '544a4c5c297e60e4ac4a5a1810df66748d908869'
      - name: 测试构建环境
        run: |
          cmake --version
          clang -v
          ninja --version
          vcpkg --version
          echo ${vcpkg_root}

  test-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: lukka/run-vcpkg@v11.5
        with:
          vcpkgCommitId: '544a4c5c297e60e4ac4a5a1810df66748d908869'
      - name: 测试构建环境
        run: |
          cmake --version
          g++ --version
          ninja --version
          vcpkg --version
          echo ${vcpkg_root}